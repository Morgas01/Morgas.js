µ.Detached:
	-	use listeners
	-	abort function -> rejects with "abort"
	-	progress / signal.progress
	-	rename "always" to something like "finish" or "end"
	-	add signal.abort
	-	add signal.scope
µ.Worker:
	-	DBWorker
	-	batch
µ.util.request:
	-	fallback urls
NEW:
	SQLiteConnector for moz extensions
	RemoteDBConnector (worker/REST)
	(Indexed)DB GUI
	simple tool for custom builds (concating files)
