Morgas.js:
	-	loadedModules
	-	remove .created statelistener
build:
	-	docs (jsDocs ?)

µ.DependencyResolver:
	-	merged packages

µ.Worker:
	-	DBWorker
	-	batch ?

µ.DB.Connector, µ.DB.Connector.*:
	-	getObjectTypes()

µ.DB.Connector.IndexedDBConnector:
	-	isAvailable()

Morgas.util.object.inputValues.js:
	-	array support
	-	dynamic option (object with getter & setter)
	-	subscribers? (zb: slider + input)

Morgas.util.Request.js:
	-	scope as param (in param object)
NodeJs/Commander:
	-	help command
	-	command in new process (multithread patch ?)
	-	command options ( -c )
File/FileHelper:
	-	copy
	-	move (copy) to other devices (drives)
	-	getRotatedFile
NEW:
	generic DB Object
	RemoteDBConnector (worker/REST)
	(Indexed)DB GUI
	simple tool for custom builds (concating files, settings, dependencies, )
	resource handler (server push?)
	translations
	translated resources
	less + minify
