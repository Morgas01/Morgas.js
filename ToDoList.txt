µ.Worker:
	-	DBWorker
	-	batch

use µ.Promise instead of µ.Detached

µ.DB.Connector, µ.DB.Connector.*:
	-	getObjectTypes()

µ.DB.Connector.IndexedDBConnector:
	-	isAvailable()
	
Morgas.util.object.inputValues.js:
	-	array support
NEW:
	SQLiteConnector for moz extensions
	generic DB Object
	RemoteDBConnector (worker/REST)
	(Indexed)DB GUI
	simple tool for custom builds (concating files)
	resource handler
	translations
	translated resources
	